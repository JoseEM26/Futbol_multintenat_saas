{
  "name": "cancha-futbol-saas",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run db:push -w @cancha/database && npm run dev -w @cancha/frontend",
    "build": "npm run build --workspaces"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "better-auth": "^1.6.9"
  }
}

